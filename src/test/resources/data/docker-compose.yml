version: '3'

services:

  mysql-mempoi:
    image: mysql:8.0.17
    environment:
      MYSQL_ROOT_PASSWORD: mempassword
      MYSQL_DATABASE: mempoi
    ports:
      - "3309:3306"
    volumes:
      - ./data:/var/lib/mysql